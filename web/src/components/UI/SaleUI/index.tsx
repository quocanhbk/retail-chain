import { Grid, Heading } from "@chakra-ui/react"

interface SaleUIProps {}

const SaleUI = ({}: SaleUIProps) => {
	return (
		<Grid w="full" h="full" placeItems="center">
			<Heading>Sale</Heading>
		</Grid>
	)
}

export default SaleUI
